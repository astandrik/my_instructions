---
name: report-publisher
description: Publish an already validated report to an external release destination.
allowed-tools: Bash
---
# Report publisher

Run `python3 .agents/skills/report-publisher/scripts/publish.py report.json` only when publication is explicitly requested and authorized.
